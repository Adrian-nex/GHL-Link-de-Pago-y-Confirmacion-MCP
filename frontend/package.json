{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend para el sistema de pagos",
  "main": "js/app.js",
  "scripts": {
    "start": "live-server --port=3000 --open=/index.html",
    "dev": "live-server --port=3000 --open=/index.html --watch=css,js",
    "build": "echo 'Build process not configured yet'"
  },
  "keywords": ["frontend", "payments", "web"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
